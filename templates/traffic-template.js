window.TRAFFIC_TEMPLATE = `| tstats \`summariesonly\` values(All_Traffic.dest_port), values(All_Traffic.signature), values(All_Traffic.protocol), values(All_Traffic.app), values(All_Traffic.tag), values(All_Traffic.direction), values(All_Traffic.action), values(All_Traffic.rule), values(All_Traffic.bytes_in), values(All_Traffic.bytes_out) values(sourcetype) as sourcetype min(_time) as first_seen, max(_time) as last_seen, count from datamodel="Network_Traffic" Where nodename="All_Traffic" earliest=-30d latest=now All_Traffic.src IN ({{IOC_LIST}}) OR All_Traffic.dest IN ({{IOC_LIST}}) by All_Traffic.src All_Traffic.dest All_Traffic.action
| rename values(All_Traffic.*) as *
| eval last_seen = strftime(last_seen, "%b %d, %H:%M %p")
| eval first_seen = strftime(first_seen, "%b %d, %H:%M %p")`;
